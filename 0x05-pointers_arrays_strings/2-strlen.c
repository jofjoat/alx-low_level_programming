#include "main.h"

/**
 * _strlen - returns the length of a string
 * @s: pointer 
 * Return: j 
 */
int _strlen(char *s)
{
    int j = 0;

    for (j = 0; s[j] != '\0'; i++)
    {}
    return (j);
}
