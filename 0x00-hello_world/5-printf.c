#include <stdio.h>

/**
 * main - program to print with the printf function
 * Return: 0 if exited properly, non-zero otherwise
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
